@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card border-danger">
                <div class="card-header bg-danger text-white">
                    <h1 class="mb-0"><i class="fas fa-exclamation-triangle mr-2"></i>Error</h1>
                </div>
                <div class="card-body">
                    <h2 class="card-title text-danger">Lo sentimos, ha ocurrido un error al procesar su solicitud.</h2>
                    <p class="card-text">Nuestro equipo técnico ha sido notificado y estamos trabajando para resolver el problema lo antes posible.</p>

                    @if (Model.ShowRequestId)
                    {
                        <p class="mt-3">
                            <strong>ID de solicitud:</strong> <code>@Model.RequestId</code>
                        </p>
                    }

                    <div class="mt-4">
                        <a href="/" class="btn btn-primary"><i class="fas fa-home mr-2"></i>Volver a la página principal</a>
                    </div>
                </div>
            </div>

            @if (Environment.GetEnvironmentVariable("ASPNETCORE_ENVIRONMENT") == "Development")
            {
                <div class="card mt-4">
                    <div class="card-header bg-warning">
                        <h3 class="mb-0">Modo de Desarrollo</h3>
                    </div>
                    <div class="card-body">
                        <p>
                            Cambiar al entorno de <strong>Desarrollo</strong> mostrará información más detallada sobre el error ocurrido.
                        </p>
                        <p>
                            <strong>El entorno de Desarrollo no debe estar habilitado para aplicaciones desplegadas.</strong>
                            Puede resultar en la exposición de información sensible de las excepciones a los usuarios finales.
                            Para depuración local, habilite el entorno de <strong>Desarrollo</strong> configurando la variable de entorno <strong>ASPNETCORE_ENVIRONMENT</strong> a <strong>Development</strong>
                            y reiniciando la aplicación.
                        </p>
                    </div>
                </div>
            }
        </div>
    </div>
</div>