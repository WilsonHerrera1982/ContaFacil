@model ContaFacil.Models.OpcionesCliente

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>OpcionesCliente</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="OpcId" />
            <div class="form-group">
                <label asp-for="OpcionId" class="control-label"></label>
                <select asp-for="OpcionId" class="form-control" asp-items="ViewBag.OpcionId"></select>
                <span asp-validation-for="OpcionId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="EmpresaId" class="control-label"></label>
                <select asp-for="EmpresaId" class="form-control" asp-items="ViewBag.EmpresaId"></select>
                <span asp-validation-for="EmpresaId" class="text-danger"></span>
            </div>
          
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
