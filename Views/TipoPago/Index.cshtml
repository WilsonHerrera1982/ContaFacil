@model IEnumerable<ContaFacil.Models.TipoPago>

@{
    ViewData["Title"] = "Index";
}

<h1>Tipos de Pago</h1>

<p>
    <a asp-action="Create">Crear Nuevo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoPago">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoPago">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoPago">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
